<%= form_tag searches_path, method: :get, id: "#{form_type}" do |f| %>
  <input type="hidden" name="article_type"  value="<%= value %>" />
  <%= link_to "#{value}", "", onclick: "$('##{form_type}').submit()" %>
<% end %>